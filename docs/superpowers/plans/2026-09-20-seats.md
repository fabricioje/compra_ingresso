# Assentos (Seat) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expor os assentos de um evento pela API e implementar o ciclo de vida da reserva (reservar, liberar, expirar).

**Architecture:** As transições de estado moram no model `Seat` (`reserve!`, `release!`, scopes), protegidas por trava de linha (`with_lock`). O `SeatsController` segue o padrão fino de `UsersController`/`EventsController`. A expiração é preguiçosa — leitura e reserva já tratam `reserved_until` vencido como disponível — e um job recorrente apenas faz higiene do banco.

**Tech Stack:** Rails 8.1 (API only), PostgreSQL 18, Minitest + fixtures, Solid Queue.

**Spec:** `docs/superpowers/specs/2026-09-20-seats-design.md`

## Global Constraints

- Ruby/Rails: app em `backend/ingressos`, `config.api_only = true`, Rails 8.1.
- Todos os comandos rodam no container, a partir da raiz do repositório:
  `docker compose exec -T -w /app/ingressos backend <comando>`.
- **Pré-requisito:** feche a conexão do DBeaver com o banco `ingressos_test`.
  `bin/rails test` faz purge do banco de teste e falha com
  `PG::ObjectInUse` enquanto houver outra sessão conectada.
- Padrão dos controllers existentes: `before_action :set_<recurso>`,
  `params.expect`, método privado `<recurso>_json`, erros como
  `{ "errors": … }`. `RecordNotFound` → 404 e `ParameterMissing` → 400 já
  são tratados no `ApplicationController`.
- Mensagens de erro de domínio em português.
- `RESERVATION_TTL` = 15 minutos.
- Um commit por task, mensagem em português, prefixo `feat:`/`test:`/`fix:`.

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `app/models/seat.rb` (modificar) | Validações, scopes de disponibilidade e as transições `reserve!`/`release!` |
| `app/controllers/seats_controller.rb` (criar) | CRUD + ações `reserve`/`release`, tradução de estado para HTTP |
| `app/jobs/release_expired_seats_job.rb` (criar) | Higiene: devolve reservas vencidas para `livre` |
| `config/routes.rb` (modificar) | Rotas aninhadas em evento com `shallow` |
| `config/recurring.yml` (modificar) | Agendamento do job em produção |
| `test/fixtures/{users,events,seats,order_items}.yml` | Base de dados de teste cobrindo os quatro estados |
| `test/models/seat_test.rb` | Regras do model |
| `test/controllers/seats_controller_test.rb` | Contrato HTTP das sete rotas |
| `test/jobs/release_expired_seats_job_test.rb` | Comportamento do job |

---

### Task 1: Fixtures base e validações do Seat

As fixtures atuais estão quebradas: `seats.yml` e `orders.yml` referenciam
`event: one` e `user: one`, mas não existem `events.yml` nem `users.yml`, e
a chave estrangeira falha ao carregar. Esta task conserta a base de teste e
entrega as validações do model.

**Files:**
- Create: `test/fixtures/users.yml`, `test/fixtures/events.yml`
- Modify: `test/fixtures/seats.yml`, `test/fixtures/order_items.yml`, `app/models/seat.rb`
- Test: `test/models/seat_test.rb`

**Interfaces:**
- Consumes: nada.
- Produces: fixtures `users(:one)`, `users(:two)`, `events(:one)`, `events(:two)`,
  `seats(:livre)`, `seats(:reservado)`, `seats(:expirado)`, `seats(:vendido)`.
  Validações em `Seat`: `number` presente e único por `event_id + sector + row`,
  `price >= 0` permitindo nulo.

- [ ] **Step 1: Escrever as fixtures**

`test/fixtures/users.yml`:

```yaml
one:
  name: Alice
  email: alice@example.com
  password_digest: <%= BCrypt::Password.create("password123") %>

two:
  name: Bob
  email: bob@example.com
  password_digest: <%= BCrypt::Password.create("password123") %>
```

`test/fixtures/events.yml`:

```yaml
one:
  name: Show A
  date: 2026-10-01
  time: 20:00
  local: Arena

two:
  name: Show B
  date: 2026-11-01
  time: 21:00
  local: Teatro
```

`test/fixtures/seats.yml` (substitui o conteúdo atual):

```yaml
livre:
  event: one
  sector: A
  row: "1"
  number: "1"
  price: 100.0
  status: 0

reservado:
  event: one
  sector: A
  row: "1"
  number: "2"
  price: 100.0
  status: 1
  user: one
  reserved_until: <%= 10.minutes.from_now.to_fs(:db) %>

expirado:
  event: one
  sector: B
  row: "1"
  number: "3"
  price: 150.0
  status: 1
  user: two
  reserved_until: <%= 10.minutes.ago.to_fs(:db) %>

vendido:
  event: two
  sector: A
  row: "1"
  number: "1"
  price: 200.0
  status: 2
  user: two
```

`test/fixtures/order_items.yml` (as referências `seat: one`/`seat: two` não
existem mais):

```yaml
one:
  order: one
  seat: vendido
  price: 200.0
```

- [ ] **Step 2: Escrever os testes de validação que falham**

`test/models/seat_test.rb` (substitui o arquivo de scaffold):

```ruby
require "test_helper"

class SeatTest < ActiveSupport::TestCase
  test "exige número" do
    seat = Seat.new(event: events(:one), sector: "C", row: "1")

    assert_not seat.valid?
    assert_includes seat.errors[:number], "can't be blank"
  end

  test "número é único por evento, setor e fileira" do
    duplicado = Seat.new(event: events(:one), sector: "A", row: "1", number: "1")

    assert_not duplicado.valid?
  end

  test "mesmo número em outro setor é válido" do
    seat = Seat.new(event: events(:one), sector: "Z", row: "1", number: "1")

    assert seat.valid?
  end

  test "mesmo número em outro evento é válido" do
    seat = Seat.new(event: events(:two), sector: "B", row: "1", number: "3")

    assert seat.valid?
  end

  test "preço não pode ser negativo" do
    seat = Seat.new(event: events(:one), sector: "C", row: "1", number: "9", price: -1)

    assert_not seat.valid?
  end

  test "preço pode ser nulo" do
    seat = Seat.new(event: events(:one), sector: "C", row: "1", number: "9")

    assert seat.valid?
  end
end
```

- [ ] **Step 3: Rodar e ver falhar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/models/seat_test.rb`
Expected: FAIL — os testes de validação passam vazios hoje, então a falha
esperada é `Expected true to be nil or false` em "exige número",
"número é único…" e "preço não pode ser negativo".

- [ ] **Step 4: Implementar as validações**

Em `app/models/seat.rb`, logo abaixo do `enum`:

```ruby
  validates :number, presence: true,
                     uniqueness: { scope: %i[event_id sector row] }
  validates :price, numericality: { greater_than_or_equal_to: 0 }, allow_nil: true
```

- [ ] **Step 5: Rodar e ver passar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/models/seat_test.rb`
Expected: PASS, 6 runs, 0 failures, 0 errors.

- [ ] **Step 6: Commit**

```bash
git add backend/ingressos/app/models/seat.rb backend/ingressos/test/fixtures backend/ingressos/test/models/seat_test.rb
git commit -m "feat: valida numero e preco do assento"
```

---

### Task 2: Disponibilidade e scopes

**Files:**
- Modify: `app/models/seat.rb`
- Test: `test/models/seat_test.rb`

**Interfaces:**
- Consumes: fixtures e validações da Task 1.
- Produces: `Seat::RESERVATION_TTL` (`15.minutes`), `Seat#disponivel?(at = Time.current) -> Boolean`,
  scopes `Seat.expiradas` e `Seat.disponiveis`.

- [ ] **Step 1: Escrever os testes que falham**

Adicione dentro de `class SeatTest`, no fim do arquivo:

```ruby
  test "assento livre está disponível" do
    assert seats(:livre).disponivel?
  end

  test "reserva dentro do prazo não está disponível" do
    assert_not seats(:reservado).disponivel?
  end

  test "reserva vencida está disponível" do
    assert seats(:expirado).disponivel?
  end

  test "assento vendido não está disponível" do
    assert_not seats(:vendido).disponivel?
  end

  test "reservado sem prazo não está disponível" do
    seat = seats(:reservado)
    seat.update_column(:reserved_until, nil)

    assert_not seat.disponivel?
  end

  test "scope expiradas traz só a reserva vencida" do
    assert_equal [ seats(:expirado) ], Seat.expiradas.to_a
  end

  test "scope disponiveis traz livre e expirado do evento" do
    disponiveis = events(:one).seats.disponiveis

    assert_includes disponiveis, seats(:livre)
    assert_includes disponiveis, seats(:expirado)
    assert_not_includes disponiveis, seats(:reservado)
  end
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/models/seat_test.rb`
Expected: FAIL com `NoMethodError: undefined method 'disponivel?'` e
`undefined method 'expiradas'`.

- [ ] **Step 3: Implementar**

Em `app/models/seat.rb`, a constante vai como primeira linha da classe e os
scopes/método ficam depois das validações:

```ruby
class Seat < ApplicationRecord
  RESERVATION_TTL = 15.minutes
```

```ruby
  scope :expiradas, -> { reservado.where(reserved_until: ...Time.current) }
  scope :disponiveis, -> { livre.or(expiradas) }

  # Um assento reservado sem prazo nunca expira sozinho: só o release! o devolve.
  def disponivel?(at = Time.current)
    return true if livre?

    reservado? && reserved_until.present? && reserved_until < at
  end
```

- [ ] **Step 4: Rodar e ver passar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/models/seat_test.rb`
Expected: PASS, 13 runs, 0 failures, 0 errors.

- [ ] **Step 5: Commit**

```bash
git add backend/ingressos/app/models/seat.rb backend/ingressos/test/models/seat_test.rb
git commit -m "feat: adiciona disponibilidade e scopes de assento"
```

---

### Task 3: Transições reserve! e release!

**Files:**
- Modify: `app/models/seat.rb`
- Test: `test/models/seat_test.rb`

**Interfaces:**
- Consumes: `disponivel?`, `RESERVATION_TTL`.
- Produces: `Seat#reserve!(user, ttl: RESERVATION_TTL) -> Boolean` e
  `Seat#release! -> Boolean`. Ambos gravam no banco, devolvem booleano (não
  levantam exceção em conflito de estado) e, na recusa, populam
  `errors[:base]`.

- [ ] **Step 1: Escrever os testes que falham**

Adicione no fim de `class SeatTest`:

```ruby
  test "reserve! reserva assento livre" do
    seat = seats(:livre)

    assert seat.reserve!(users(:two))
    assert seat.reservado?
    assert_equal users(:two), seat.user
    assert seat.reserved_until > Time.current
  end

  test "reserve! respeita o ttl informado" do
    seat = seats(:livre)

    freeze_time do
      seat.reserve!(users(:two), ttl: 5.minutes)

      assert_equal 5.minutes.from_now.to_i, seat.reserved_until.to_i
    end
  end

  test "reserve! recusa assento com reserva válida e preserva o dono" do
    seat = seats(:reservado)

    assert_not seat.reserve!(users(:two))
    assert_equal users(:one), seat.reload.user
    assert_not_empty seat.errors[:base]
  end

  test "reserve! assume assento com reserva vencida" do
    seat = seats(:expirado)

    assert seat.reserve!(users(:one))
    assert_equal users(:one), seat.reload.user
    assert seat.reserved_until > Time.current
  end

  test "reserve! recusa assento vendido" do
    seat = seats(:vendido)

    assert_not seat.reserve!(users(:one))
    assert seat.reload.vendido?
  end

  test "release! libera assento reservado" do
    seat = seats(:reservado)

    assert seat.release!
    assert seat.reload.livre?
    assert_nil seat.user
    assert_nil seat.reserved_until
  end

  test "release! é idempotente em assento livre" do
    seat = seats(:livre)

    assert seat.release!
    assert seat.reload.livre?
  end

  test "release! recusa assento vendido" do
    seat = seats(:vendido)

    assert_not seat.release!
    assert seat.reload.vendido?
    assert_not_empty seat.errors[:base]
  end
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/models/seat_test.rb`
Expected: FAIL com `NoMethodError: undefined method 'reserve!'`.

- [ ] **Step 3: Implementar**

No fim de `app/models/seat.rb`, depois de `disponivel?`:

```ruby
  # with_lock recarrega o registro dentro de um SELECT ... FOR UPDATE, então
  # duas requisições simultâneas no mesmo assento são serializadas: a segunda
  # só lê o estado depois que a primeira comitou.
  def reserve!(user, ttl: RESERVATION_TTL)
    with_lock do
      unless disponivel?
        errors.add(:base, "Assento indisponível")
        next false
      end

      update(status: :reservado, user: user, reserved_until: ttl.from_now)
    end
  end

  def release!
    with_lock do
      if vendido?
        errors.add(:base, "Assento vendido não pode ser liberado")
        next false
      end

      update(status: :livre, user: nil, reserved_until: nil)
    end
  end
```

`next false` sai do bloco devolvendo `false`; `with_lock` devolve o valor do
bloco, então o método inteiro devolve booleano.

- [ ] **Step 4: Rodar e ver passar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/models/seat_test.rb`
Expected: PASS, 21 runs, 0 failures, 0 errors.

- [ ] **Step 5: Commit**

```bash
git add backend/ingressos/app/models/seat.rb backend/ingressos/test/models/seat_test.rb
git commit -m "feat: adiciona reserva e liberacao de assento"
```

---

### Task 4: Rotas e CRUD do SeatsController

**Files:**
- Create: `app/controllers/seats_controller.rb`, `test/controllers/seats_controller_test.rb`
- Modify: `config/routes.rb`
- Test: `test/controllers/seats_controller_test.rb`

**Interfaces:**
- Consumes: validações e `disponivel?` do model.
- Produces: helpers `event_seats_url(event)` e `seat_url(seat)`; o payload
  JSON do assento com as chaves `id, event_id, sector, row, number, price,
  status, reserved_until, user_id, created_at, updated_at, disponivel`.

- [ ] **Step 1: Escrever os testes que falham**

`test/controllers/seats_controller_test.rb`:

```ruby
require "test_helper"

class SeatsControllerTest < ActionDispatch::IntegrationTest
  test "index lista os assentos do evento" do
    get event_seats_url(events(:one))

    assert_response :success
    assert_equal events(:one).seats.count, JSON.parse(response.body).size
  end

  test "index expõe o campo disponivel" do
    get event_seats_url(events(:one))

    corpo = JSON.parse(response.body).find { |seat| seat["id"] == seats(:expirado).id }

    assert corpo["disponivel"]
  end

  test "index filtra por status" do
    get event_seats_url(events(:one)), params: { status: "livre" }

    assert_response :success
    assert_equal [ seats(:livre).id ], JSON.parse(response.body).map { |seat| seat["id"] }
  end

  test "index filtra por setor" do
    get event_seats_url(events(:one)), params: { sector: "B" }

    assert_equal [ seats(:expirado).id ], JSON.parse(response.body).map { |seat| seat["id"] }
  end

  test "index recusa status desconhecido" do
    get event_seats_url(events(:one)), params: { status: "banana" }

    assert_response :unprocessable_entity
  end

  test "index de evento inexistente responde 404" do
    get event_seats_url(event_id: 999_999)

    assert_response :not_found
  end

  test "create cria assento no evento" do
    assert_difference "Seat.count", 1 do
      post event_seats_url(events(:one)),
           params: { seat: { sector: "C", row: "2", number: "10", price: 80.0 } },
           as: :json
    end

    assert_response :created
    corpo = JSON.parse(response.body)

    assert_equal "livre", corpo["status"]
    assert_equal events(:one).id, corpo["event_id"]
  end

  test "create sem número responde 422" do
    post event_seats_url(events(:one)), params: { seat: { sector: "C", row: "2" } }, as: :json

    assert_response :unprocessable_entity
  end

  test "create ignora status e user_id enviados pelo cliente" do
    post event_seats_url(events(:one)),
         params: { seat: { sector: "C", row: "3", number: "11", status: "vendido", user_id: users(:one).id } },
         as: :json

    assert_response :created
    corpo = JSON.parse(response.body)

    assert_equal "livre", corpo["status"]
    assert_nil corpo["user_id"]
  end

  test "show devolve o assento" do
    get seat_url(seats(:livre))

    assert_response :success
    assert_equal seats(:livre).id, JSON.parse(response.body)["id"]
  end

  test "update altera os dados do assento" do
    patch seat_url(seats(:livre)), params: { seat: { price: 123.45 } }, as: :json

    assert_response :success
    assert_equal "123.45", JSON.parse(response.body)["price"]
  end

  test "destroy remove assento livre" do
    assert_difference "Seat.count", -1 do
      delete seat_url(seats(:livre))
    end

    assert_response :no_content
  end

  test "destroy recusa assento vendido" do
    assert_no_difference "Seat.count" do
      delete seat_url(seats(:vendido))
    end

    assert_response :unprocessable_entity
  end

  test "destroy recusa assento com item de pedido" do
    OrderItem.create!(order: orders(:one), seat: seats(:livre), price: 100.0)

    assert_no_difference "Seat.count" do
      delete seat_url(seats(:livre))
    end

    assert_response :unprocessable_entity
  end
end
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/controllers/seats_controller_test.rb`
Expected: FAIL com `NameError: undefined local variable or method 'event_seats_url'`.

- [ ] **Step 3: Declarar as rotas**

Em `config/routes.rb`, troque a linha `resources :events` por:

```ruby
  resources :events do
    resources :seats, shallow: true do
      member do
        post :reserve
        post :release
      end
    end
  end
```

- [ ] **Step 4: Implementar o controller (CRUD)**

`app/controllers/seats_controller.rb`:

```ruby
class SeatsController < ApplicationController
  before_action :set_event, only: %i[index create]
  before_action :set_seat, only: %i[show update destroy]

  # GET /events/:event_id/seats
  def index
    seats = @event.seats.order(:id)

    if params[:status].present?
      unless Seat.statuses.key?(params[:status])
        return render json: { error: "status inválido: #{params[:status]}" },
                      status: :unprocessable_entity
      end

      seats = seats.where(status: params[:status])
    end

    seats = seats.where(sector: params[:sector]) if params[:sector].present?

    render json: seats.map { |seat| seat_json(seat) }
  end

  # GET /seats/:id
  def show
    render json: seat_json(@seat)
  end

  # POST /events/:event_id/seats
  def create
    seat = @event.seats.new(seat_params)

    if seat.save
      render json: seat_json(seat), status: :created
    else
      render json: { errors: seat.errors }, status: :unprocessable_entity
    end
  end

  # PATCH/PUT /seats/:id
  def update
    if @seat.update(seat_params)
      render json: seat_json(@seat)
    else
      render json: { errors: @seat.errors }, status: :unprocessable_entity
    end
  end

  # DELETE /seats/:id
  def destroy
    if @seat.vendido?
      return render json: { errors: { base: [ "Assento vendido não pode ser removido" ] } },
                    status: :unprocessable_entity
    end

    if @seat.destroy
      head :no_content
    else
      render json: { errors: @seat.errors }, status: :unprocessable_entity
    end
  end

  private

  def set_event
    @event = Event.find(params[:event_id])
  end

  def set_seat
    @seat = Seat.find(params[:id])
  end

  # status, user_id e reserved_until ficam de fora de propósito: o estado do
  # assento só muda por reserve!/release!.
  def seat_params
    params.expect(seat: %i[sector row number price])
  end

  def seat_json(seat)
    seat.as_json(only: %i[id event_id sector row number price status reserved_until user_id created_at updated_at])
        .merge("disponivel" => seat.disponivel?)
  end
end
```

- [ ] **Step 5: Rodar e ver passar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/controllers/seats_controller_test.rb`
Expected: PASS, 15 runs, 0 failures, 0 errors.

- [ ] **Step 6: Conferir a tabela de rotas**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails routes -c seats`
Expected: sete linhas — `GET/POST /events/:event_id/seats`,
`GET/PATCH/PUT/DELETE /seats/:id`, `POST /seats/:id/reserve`,
`POST /seats/:id/release`.

- [ ] **Step 7: Commit**

```bash
git add backend/ingressos/config/routes.rb backend/ingressos/app/controllers/seats_controller.rb backend/ingressos/test/controllers/seats_controller_test.rb
git commit -m "feat: adiciona rotas e CRUD de assentos"
```

---

### Task 5: Endpoints de reserva e liberação

**Files:**
- Modify: `app/controllers/seats_controller.rb`
- Test: `test/controllers/seats_controller_test.rb`

**Interfaces:**
- Consumes: `Seat#reserve!`, `Seat#release!`, `seat_json`.
- Produces: `POST /seats/:id/reserve` (body `{"user_id": <id>}`) e
  `POST /seats/:id/release`. Sucesso 200 com o assento; conflito de estado
  409; `user_id` ausente 400; usuário inexistente 404.

- [ ] **Step 1: Escrever os testes que falham**

Adicione no fim de `class SeatsControllerTest`:

```ruby
  test "reserve reserva assento livre" do
    post reserve_seat_url(seats(:livre)), params: { user_id: users(:two).id }, as: :json

    assert_response :success
    corpo = JSON.parse(response.body)

    assert_equal "reservado", corpo["status"]
    assert_equal users(:two).id, corpo["user_id"]
    assert_not corpo["disponivel"]
    assert seats(:livre).reload.reservado?
  end

  test "reserve de assento já reservado responde 409" do
    post reserve_seat_url(seats(:reservado)), params: { user_id: users(:two).id }, as: :json

    assert_response :conflict
    assert_equal users(:one).id, seats(:reservado).reload.user_id
  end

  test "reserve de assento com reserva vencida funciona" do
    post reserve_seat_url(seats(:expirado)), params: { user_id: users(:one).id }, as: :json

    assert_response :success
    assert_equal users(:one).id, seats(:expirado).reload.user_id
  end

  test "reserve sem user_id responde 400" do
    post reserve_seat_url(seats(:livre)), params: {}, as: :json

    assert_response :bad_request
  end

  test "reserve com usuário inexistente responde 404" do
    post reserve_seat_url(seats(:livre)), params: { user_id: 999_999 }, as: :json

    assert_response :not_found
    assert seats(:livre).reload.livre?
  end

  test "release libera assento reservado" do
    post release_seat_url(seats(:reservado))

    assert_response :success
    corpo = JSON.parse(response.body)

    assert_equal "livre", corpo["status"]
    assert_nil corpo["user_id"]
    assert_nil corpo["reserved_until"]
  end

  test "release de assento vendido responde 409" do
    post release_seat_url(seats(:vendido))

    assert_response :conflict
    assert seats(:vendido).reload.vendido?
  end
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/controllers/seats_controller_test.rb`
Expected: FAIL — `AbstractController::ActionNotFound: The action 'reserve' could not be found for SeatsController`.

- [ ] **Step 3: Implementar as ações**

Em `app/controllers/seats_controller.rb`, inclua `reserve` e `release` no
`before_action :set_seat`:

```ruby
  before_action :set_seat, only: %i[show update destroy reserve release]
```

Adicione as ações depois de `destroy`, antes do `private`:

```ruby
  # POST /seats/:id/reserve
  def reserve
    user = User.find(params.require(:user_id))

    if @seat.reserve!(user)
      render json: seat_json(@seat)
    else
      render json: { errors: @seat.errors }, status: :conflict
    end
  end

  # POST /seats/:id/release
  def release
    if @seat.release!
      render json: seat_json(@seat)
    else
      render json: { errors: @seat.errors }, status: :conflict
    end
  end
```

`params.require(:user_id)` levanta `ParameterMissing` quando ausente, que o
`ApplicationController` já traduz em 400; `User.find` levanta
`RecordNotFound`, traduzido em 404.

- [ ] **Step 4: Rodar e ver passar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/controllers/seats_controller_test.rb`
Expected: PASS, 22 runs, 0 failures, 0 errors.

- [ ] **Step 5: Commit**

```bash
git add backend/ingressos/app/controllers/seats_controller.rb backend/ingressos/test/controllers/seats_controller_test.rb
git commit -m "feat: adiciona endpoints de reserva e liberacao"
```

---

### Task 6: Job de expiração

**Files:**
- Create: `app/jobs/release_expired_seats_job.rb`, `test/jobs/release_expired_seats_job_test.rb`
- Modify: `config/recurring.yml`

**Interfaces:**
- Consumes: `Seat.expiradas`, `Seat#release!`.
- Produces: `ReleaseExpiredSeatsJob.perform_now` / `.perform_later`, sem argumentos.

- [ ] **Step 1: Escrever o teste que falha**

`test/jobs/release_expired_seats_job_test.rb`:

```ruby
require "test_helper"

class ReleaseExpiredSeatsJobTest < ActiveJob::TestCase
  test "devolve para livre as reservas vencidas" do
    ReleaseExpiredSeatsJob.perform_now

    expirado = seats(:expirado).reload

    assert expirado.livre?
    assert_nil expirado.user
    assert_nil expirado.reserved_until
  end

  test "não toca em reserva dentro do prazo nem em vendido" do
    ReleaseExpiredSeatsJob.perform_now

    assert seats(:reservado).reload.reservado?
    assert_equal users(:one), seats(:reservado).user
    assert seats(:vendido).reload.vendido?
  end
end
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/jobs/release_expired_seats_job_test.rb`
Expected: FAIL com `NameError: uninitialized constant ReleaseExpiredSeatsJob`.

- [ ] **Step 3: Implementar o job**

`app/jobs/release_expired_seats_job.rb`:

```ruby
# Higiene do banco: a expiração em si já é tratada na leitura (Seat#disponivel?),
# então atrasar ou perder uma execução não muda o comportamento da API.
class ReleaseExpiredSeatsJob < ApplicationJob
  queue_as :default

  def perform
    Seat.expiradas.find_each { |seat| seat.release! }
  end
end
```

- [ ] **Step 4: Rodar e ver passar**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test test/jobs/release_expired_seats_job_test.rb`
Expected: PASS, 2 runs, 0 failures, 0 errors.

- [ ] **Step 5: Agendar em produção**

Em `config/recurring.yml`, dentro do bloco `production:` existente, abaixo de
`clear_solid_queue_finished_jobs`:

```yaml
  release_expired_seats:
    class: ReleaseExpiredSeatsJob
    schedule: every minute
```

- [ ] **Step 6: Rodar a suíte inteira**

Run: `docker compose exec -T -w /app/ingressos backend bin/rails test`
Expected: PASS, 45 runs, 0 failures, 0 errors (21 de model, 22 de requisição, 2 de job).

- [ ] **Step 7: Rodar o rubocop**

Run: `docker compose exec -T -w /app/ingressos backend bin/rubocop app/models/seat.rb app/controllers/seats_controller.rb app/jobs/release_expired_seats_job.rb`
Expected: no offenses. Se houver, corrija e rode de novo.

- [ ] **Step 8: Commit**

```bash
git add backend/ingressos/app/jobs/release_expired_seats_job.rb backend/ingressos/test/jobs/release_expired_seats_job_test.rb backend/ingressos/config/recurring.yml
git commit -m "feat: libera reservas vencidas em job recorrente"
```
